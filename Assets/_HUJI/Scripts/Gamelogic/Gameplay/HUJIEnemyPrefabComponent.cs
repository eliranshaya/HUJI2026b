namespace HUJI.Gamelogic
{
    public class HUJIEnemyPrefabComponent : HUJIPoolPrefab
    {
        public virtual void SetEnemy(int level, EnemyConfig enemyConfig)
        {
            // EnemyLevel = level;
            // EnemyConfig = enemyConfig;
            //
            // UniqueId = LTCGameManagerComponent.Instance.GetUniqueId();
            // string uniqueIdString = UniqueId.ToString();
            // gameObject.name = uniqueIdString;
            // HealthCollider.enabled = true;
            //
            // Agent.enabled = true;
            //
            // LTCGameManagerComponent.Instance.EnemySpawner.AddEnemy(uniqueIdString, this);
            //
            // EnemyAnimation = new(this);
            // EnemyHealth = new(this);
            // EnemyMovement = new(this);
            // EnemyAttack = new(this);
        }
    }
}